Пару общих моментов:
    1. По всему проекту прыгает форматирование, неудобно читать, старайся делать отступы в .ts файлах
    между объявлением/инициализацией свойств/методов;
        >> это решается помощью ng lint, я его добавил в проект, надо поправить все ошибки (или правила линтера). Кроме того, удобно сделать автоформатирование файлов

    2. Где свойста публичные, лучше тоже писать public, а не просто их оставлять
        >> это в monster проекте так принято, по умолчанию этого нет, лично мне показалось удобнее, но вкусовщина необязательная

    3. Везде лучше объявлять сначала public, потом protected, потом private
        >> настраивается жёстко через lint конфиг файлом

    4. Строки с роутами везде лучше вынести в enum
        >> +, вообще любое дублирование и магические строки/цифры необходимо избегать и иметь в одном экземпляре

    5. У названий интерфейсов можно добавлять префикс I, чтобы обозначить, что это интерфейс
     (типо IArticle), но это необязательно, просто так часто делают
        >> это правило устарело, но считаю его очень удобным, пришло из C# мира, спорный момент

    6. ангуляр не последний, в 16ом появилось много клёвых штук

    7. смотри на resolvers, guards, они в старом формате, уже есть новое апи
